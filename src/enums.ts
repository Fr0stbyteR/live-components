enum ELiveParamType {
    FLOAT = 0,
    INT = 1,
    ENUM = 2
}

enum ELiveParamUnitStyle {
    INT = 0,
    FLOAT = 1,
    TIME = 2,
    HERTZ = 3,
    DECIBEL = 4,
    PERCENT = 5,
    PAN = 6,
    SEMITONES = 7,
    MIDI = 8,
    CUSTOM = 9,
    NATIVE = 10
}
